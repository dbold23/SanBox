# P0_STATUS — every prerequisite, who owns it, what it blocks

P0 items are from `docs/sevengill-canonical-reid/03-candidate-approaches.md` §P0. **Owner vocabulary:**
`code-done` = the artifact exists in this repo and is testable; `needs-PI` = a human decision, a send,
or a run on a machine with open egress; `needs-partner` = waiting on someone outside the lab.
Nothing here is blocked on more analysis.

| # | P0 item | Owner | Artifact / evidence | Blocking what | Next action |
|---|---|---|---|---|---|
| **a1** | `species` column across `shark_catalog` and the annotation / track / consensus tables in `SharkScarAnnotator` | **code-done** → needs-PI to apply | `phase1b/p0-species-migration/` — 3 ordered patches + `APPLY.md`, `git am` verified against a fresh clone of `main` | **Everything.** Without it there is no field in which a sevengill record can be stored, and every downstream split is unsound | Apply the three patches on a branch, run the 53-file pytest suite, merge. No migration script needed (see `APPLY.md`) |
| **a2** | Hexanchiform keypoint schema — seven gill slits bracketed, `second_dorsal_*` gone, dorsal repositioned over the pelvics, pelvic/cloaca + precaudal pit + midline chain added | **code-done** → needs-partner sign-off | `phase1b/p0-sevengill-schema/` — Schema S1 (30 pts), loader + validator, 15 repo-suite tests in the patch + 44 design-artifact tests in phase1b/p0-sevengill-schema/tests/; `keypoints_16.yaml` untouched | Sevengill keypoint annotation; the detector retrain; Approach 2's landmark anchoring; the `bbox_head`/`bbox_headless` split in `DRIFT_PROTOCOL.md` §3, **and** the separate `head_patch` crop (S1 `chart.head_patch_bounds`, naris → gill slit 1) that the drift protocol needs before it can compare against the incumbent's own region | Send `OUTREACH_EBERT.md` with the YAML + `RATIONALE.md` attached. Freeze S1 only after his review |
| **a3** | Melops keypoint-count discrepancy (source tuple has 16 entries; the *Scientific Data* text is reported as 11) | **needs-PI** | `01-evidence-and-answers.md` Q3 — flagged "do not publish either figure" | Nothing structural; it is a citation-hygiene blocker on anything we write about Melops keypoints | Open the Zenodo 17404087 annotation file from an open-egress machine and settle it. 10 minutes |
| **a4** | `shark_smpl.py:109-111` — the "91 dense surface landmarks" are `torch.linspace` one-hot over vertex indices, i.e. neither correspondence nor anatomy | **needs-PI** (approach-dependent) | Verified by inspection in the scan | Approach 2 only (it replaces them with a tube chart; Approaches 1 and 3 delete them) | No action this quarter. Decide when Approach 2 is funded; do not "fix" them in the meantime |
| **b1** | DigitalLife3D template licence — read the actual terms | **decided** (owner, 2026-09-01: personal non-commercial research use under CC-BY-NC — gate cleared; see the owner-decision block in the licence doc) | `DIGITALLIFE3D_LICENCE.md` §2 row 5 — `[SEARCH]`, single source; `sketchfab.com` egress-blocked here | Approach 2 (all of it is modification of that mesh) | Open the two Sketchfab model pages, paste the licence string verbatim into §2, upgrade the row to `[PRIMARY]` |
| **b2** | DigitalLife3D — **written** permission for research use, retopology, derivative redistribution, publication figures | **optional courtesy** (owner decision: not blocking; recommended before any publication that redistributes a derived mesh) | `DIGITALLIFE3D_LICENCE.md` §5 — 147-word draft, contact route `duncan@umass.edu` `[SEARCH]` | Approach 2 authorisation; and open release of any derived sevengill template (NC propagates) | Send the §5 draft. Reply may take weeks; it is off the critical path this quarter, which is exactly why it should go out now |
| **b3** | Latent licence conflict: `shark-pose-3d` declares MIT repo-wide while its template mesh is CC-BY-NC | **needs-PI** | `[PRIMARY]` — `README.md` "License: MIT"; `tasks/progress.md:16` "Model 92A … CC-BY-NC"; `git ls-files` confirms no mesh is committed today | Nothing yet; it becomes real the moment a mesh-derived `.obj`/`.npy`/render set is committed or released | Add `ASSETS.md` (mesh derivatives = CC BY-NC 4.0, Digital Life + SOSF credited, MIT covers code only) and extend the OBJ header line in both export scripts to carry the licence |
| **c1** | **David Ebert / Pacific Shark Research Center** — schema sign-off, ontogenetic-stability opinion, specimen/imagery pointers | **needs-PI to send** → then needs-partner | `OUTREACH_EBERT.md` — draft, 236 words, affiliation `[SEARCH]`-verified, **email address deliberately left as a placeholder** (`mlml.sjsu.edu` egress-blocked) | a2 sign-off; the P0-d premise question; possible imagery | Look up the address on the PSRC people page, fill `[PI NAME]`, attach the schema, send. Spec calls this "the single cheapest high-value action in the whole plan" |
| **c2** | **Ocean Sanctuaries / Sharkbook** — access to the confirmed multi-year resight subset | **needs-PI to send** → then needs-partner | `OUTREACH_OCEAN_SANCTUARIES.md` — draft, 288 words; leadership currently ambiguous (Bear vs Lloyd) `[SEARCH, conflicting]` | **d1 entirely**, and the fastest realistic route to a 40+ individual sevengill catalogue | Confirm the current lead, prepare the one-page data agreement, send via the site contact form. Send ~1–2 weeks after c1 so Ebert can vouch. The ask now includes the **per-link discovery route** (manual vs algorithm-suggested) — `DRIFT_PROTOCOL.md` §7.11's control on the MiewID-mediated selection bias; if they cannot supply it, that is a stated limitation, not a blocker |
| **c3** | **Wild Me / Conservation X Labs (Sharkbook.ai)** — platform-side export help, and the MiewID terms | **needs-partner**, conditional | `wbia-plugin-miew-id` has **no OSI licence file**, only "Copyright Conservation X Labs 2025" `[PRIMARY, prior scan]`; Sharkbook runs MiewID and carries left/right viewpoint `[SEARCH]` | Any Phase 1B arm built on MiewID weights; export mechanics if c2 asks us to route through the platform | Do **not** contact ahead of c2 — the data is Ocean Sanctuaries', not the platform's. Separately, settle MiewID terms with CXL before MiewID appears in any published result |
| **c4** | **OSU Big Fish Lab (Chapple / McInturf)** and **Otago** (stereo-BRUV, <1% calibration error on free-swimming sevengills, 78 individuals) | **needs-PI** | `03-candidate-approaches.md` P0-c and Approach 2 data requirements `[SEARCH]` | A second catalogue if c2 delivers <40 individuals; metric 3D ground truth for Approach 2 | No draft written. Raise with the PI as the named fallback in `DRIFT_PROTOCOL.md` §1 |
| **c5** | **Monterey Bay Aquarium** — captive sevengills, the only setting with certain identity at known intervals | **needs-PI** | `03-candidate-approaches.md` P0-c `[SEARCH]` | The cleanest possible pattern-stability experiment, and it is local | No draft written. It is the controlled complement to d1's archival measurement — worth a decision on whether to open it in parallel |
| **d1** | Pattern-drift measurement on confirmed multi-year resights | **code-done** (protocol + analysis) → needs-partner (data) | `DRIFT_PROTOCOL.md` — pre-registered; runs on `prototypes/01-melops-ablation/diagnose.py` + `readout_length_controlled.py` with **zero new analysis code**, via one `metadata.csv` in the Melops contract | The whole programme's premise. RED-A on both arms kills Approaches 1B, 2 and 3 together for the price of one export | Nothing until c2 lands. On arrival: run the §3 contract gate, then the §8 runbook. Decisions read on the **pooled ≥181 d** kill window (§5.3), and every verdict ships with the discovery-route strata and the "upper bound on stability" statement (§4 rule 8, §7.11) |
| **d2** | Rectification test — raw crop vs prototype-02 midline rectification, ≥3 Rank-1 points **overall** (the curvature-tercile readout is descriptive only) | **code-done** → needs-partner (data) | `DRIFT_PROTOCOL.md` §6; `prototypes/02-centerline-chart/` (25 tests; measured bend residual 0.07% BL, strain residual ~0.7% BL) | The Phase 1B secondary arm, and the first measurement of what rectification buys on a **bending** body — the thing Melops structurally could not test | Rides on d1's export. No separate ask |

## Critical path, in one sentence

**c1 and c2 are the only things on it** — a1 is done and merely needs applying, a2 needs one expert's
45 minutes, and every measurement in d1/d2 is already written and tested; what the programme lacks is
sevengill photographs with confirmed identities and dates.

## This week

1. Apply the a1 patches and merge (half a day, no external dependency).
2. Fill in the Ebert address, attach Schema S1, send c1.
3. Read the Sketchfab licence string (b1) — five minutes, and it upgrades P0-b from AMBER on its own.
4. Send b2, because the reply is slow and the email is free.
5. Prepare the one-page data agreement so c2 can go out next week without a delay — and put the
   per-link discovery-route field (manual vs algorithm-suggested) into the ask while drafting it.
